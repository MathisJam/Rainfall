// G PAS COMPRIS CE KON DOIT METTRE DANS SOURCE JE VERRAIS PLUS TARD
// FAUT REFAIRE LE CODE DU BINAIRE COMME ON L'A COMPRIS AVEC LE DISASS ??

#include <unistd.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
    int truc = 0;
    truc = atoi(argv[1]);
    if (truc != 423)
    {
        write(1, "No!\n", 4);
        return 0;
    }
    else
    {
    }
}
